services:
  - type: web
    name: velorent-api
    runtime: node
    rootDir: apps/backend
    buildCommand: cd ../.. && npm install && npm run build --workspace=backend
    startCommand: node dist/index.js
    envVars:
    - key: NODE_ENV
      value: production
    - key: PORT
      value: 4000